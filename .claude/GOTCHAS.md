# Soccer Stats — GOTCHAS (don't repeat these coding mistakes)

2026-06-22 — Dashboard `allMatches`/`matches` is only the loaded slate (current view), NOT full history; never compute multi-match aggregates (per-team records, all-time tallies) by scanning it client-side — precompute into `display_summary` server-side (see `headlineSummary` in precompute_display_markets.mjs) or read `data.allTimeSummary` — app/dashboard/page.jsx
2026-06-23 — A match DELAY (lightning/weather) desyncs the routine's FT detection and can freeze an in-play/partial score as final: France settled FT 1-0 from a thin Flashscore capture (only corners+cards, no shots/possession/xG) while the real result was 3-0 (ESPN FT) — flipped Over 2.5 goals miss→hit. ESPN is the authoritative final; thin actuals (cards/corners only, no core stats) signal a premature capture; correct via a `manualResultImports` Firestore doc (set matchId for exact targeting) → apply_manual_result_imports.mjs + upload_match_data_to_firestore.mjs (precomputes + pushes; no app redeploy) — scripts/soccer_routine.py settlement
2026-06-23 — Removing a `display_summary` field in precompute and uploading BEFORE deploying the app that stopped reading it leaves a live gap: dropping `cardsRecord` + uploading made the compact-row record column blank on the still-deployed old app (which read `cardsRecord`). Ship the precompute field change and its app reader together (deploy the rollout alongside the upload) — scripts/precompute_display_markets.mjs ↔ app/dashboard/page.jsx
2026-06-23 — Per-match prediction scorecard ("X/Y hit") = `display_summary.headlineSummary` (hits/settled over the 5 standard markets winner/btts/goals/cards/corners). The old `cardsRecord` (a two-team cards track-record) was removed because, labelled "Cards" next to the per-match Pick, it read as "this match's cards" and exceeded the ~6-market max (e.g. 7/7). Don't put an aggregate team record in a per-match slot — app/dashboard/page.jsx ↔ scripts/precompute_display_markets.mjs
2026-06-22 — Same team has DIFFERENT `team_id` across providers (ESPN Spain=164 vs SofaScore Spain=4698); matching team history by `team_id` alone drops most of it (gave 1/1 instead of 5/6) — match by id OR normalized name — scripts/precompute_display_markets.mjs
2026-06-22 — Precomputed `allTimeSummary.review.all` keys the cards row `'cards'` (upload_match_data_to_firestore.mjs `summarizeReviewRows`), but client `summarizeResultsByMarket` uses `'ou_cards'`; match either key when reading review rows — app/dashboard/page.jsx ↔ scripts/upload_match_data_to_firestore.mjs
