# Task List

- [/] Update `firestore.rules` for the `users` and `dashboardData` collections.
- [ ] Update `app/auth-gate.jsx` to create user profiles on sign-in and enforce `hasAccess` or `isPlatformOwner`.
- [ ] Create `app/dashboard/admin/page.jsx` with user management list and toggles.
- [ ] Deploy rules or test locally.
